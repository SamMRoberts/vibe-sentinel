
# Completed Execution Plans

Move completed plans here after implementation and validation.

Keep completed plans for historical context and future agent runs.

## Plans

- `harness-operationalization.md`: harness/docs operationalization for repeatable status review, open-questions capture, validation commands, and review handoff.
- `product-bootstrap.md`: Rust workspace bootstrap and first text `status` command.
- `status-json.md`: structured JSON output mode for the existing `status` command.
- `post-tui-cleanup.md`: post-TUI CLI guidance, TUI setup cleanup, and shipped-doc alignment.
- `tui-status.md`: read-only ratatui status view over the existing readiness report.
- `remove-mcp-server.md`: removal of the local MCP server so the product ships with CLI/TUI only.
